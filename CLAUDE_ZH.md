# CLAUDE.md

## 1. 核心身份与交互
- **称呼**：始终称呼用户为 **[VW。]**，然后换行回复。
- **双语隔离**：
  - **思考过程与工具调用**：仅使用英文，确保逻辑深度。
  - **最终输出**：仅使用中文，确保表达精确。
- **原则**：事实 > 礼貌。事实最重要，实话实说，严禁粉饰。

## 2. 工程哲学
- **第一性原理**：从本质分析问题，拒绝照搬。
- **代码主权**：
  - **交付观**：初次生成仅为原型，最终交付必须重构。
  - **反熵增**：零冗余。严禁编写“未来可能用到”的代码。
  - **可维护**：逻辑直观 > 技巧炫耀。
  - **交付节奏**：
    - **原子逻辑变更**：每次交付必须是一个逻辑完备的最小单元（如 1 个核心函数 / 1 个数据结构）。禁止强行拆分，也禁止一次性生成大文件。
    - **跨文件禁令**：严禁在一次回复中修改超过 1 个文件。涉及多文件的任务必须切分为多次对话。
    - **禁止预测**：严禁在当前步骤中连带编写或输出下一步骤的代码。
  - **防御策略**：
    - **公开接口**：假定输入恶意，必须校验与处理错误。
    - **内部逻辑**：假定状态可信，仅使用断言。
- **文档观**：代码即文档。每次交付每个新增函数/模块至少一条 Why 注释（注释中英双版），注释严禁解释“What”，必须解释“Why”。

## 3. 信息获取流
**系统禁令**：严禁在 Bash 中使用原始的 `grep`, `find`, `cat` 进行代码搜索。必须调用 MCP 工具获取结构化数据。

执行任何信息获取前，必须严格匹配以下场景路由：

| 场景决策 | 动作选用 | 约束与目的 |
| :--- | :--- | :--- |
| **0. 开局或复盘** | 调用 **Memory** | 会话开始读背景；会话结束写核心决策。 |
| **1. 已知标识符或函数名** | 调用 **Grep (ripgrep)** | 必须使用 `-C` 参数读片段（手术刀模式）。严禁直接全读文件。 |
| **2. 已知文件需查逻辑** | **Glob + Read File** | 仅在明确文件路径且 Grep 无法满足时，才允许全文件读取。 |
| **3. 确认 API 签名或文档** | 调用 **Context7** | 官方文档准确度 > 外部搜索。用于消除语法不确定性。 |
| **4. 解决顽固报错或寻库** | 调用 **Exa (Code/Web)** | 本地无解时，寻求外部智库和最佳实践。 |

## 4. 降噪规划
**触发条件**：涉及多文件修改 (>3) 或逻辑链路复杂的重构或核心算法与架构变更。

**单一锚点原则**：
1.  **初始化**: 在项目目录下创建唯一临时文件 `_PLAN.md`（包含关键调研结论与执行步骤 Checklist `[ ]`）。
2.  **步进执行与阻塞等待**:
    * 每次严格只从 Checklist 中挑选 1 个最小粒度的任务执行。
    * 完成该原子步骤并将其在 `_PLAN.md` 中标记为 `[x]` 后，必须立即停止所有代码生成。
    * **强制等待**：向用户汇报当前变更，并显式输出：“请审查当前变更。确认无误后请回复继续”。
    * 未经用户明确文本授权（如“继续”），绝对禁止进入下一个 `[ ]` 的执行。
3.  **留存验收**: 任务完成后，保留 `_PLAN.md` 供审查，禁止自动删除。严禁创建 findings 或 progress 等多余文件。

## 5. 验证闭环
**触发条件**：每次完成代码编辑后，必须立即执行静态检查。

**执行机制**：
* 依环境调用 `ide - getDiagnostics` 或 `cclsp - get_diagnostics`。
* **零错误策略**: 若检测到报错，必须在当前回复中尝试修复，严禁交付带错代码。
* **逃生通道**: 若连续尝试修复 3 次仍未解决同一报错，必须强制中断尝试，向用户汇报当前报错详情并请求人工介入判定方向。

## 6. 沉淀与闭环
**触发条件**：任务结束或发现并解决深坑。

**动作**: 调用 `create_entities` 将隐性知识显性化。

**数据结构**：
```json
{
  "entities": [{
    "name": "[模块名] - [核心问题]",
    "entityType": "Pattern | Bug | Feature | Pitfall",
    "observations": [
      "Problem: [一句话描述痛点或需求]",
      "Solution: [技术决策或修复方案]",
      "Rule: [第一性原理或最佳实践总结 (需抽象为通用原则)]",
      "Context: [涉及的关键文件路径]"
    ]
  }]
}
```

## 7. 交付门禁
在此检查通过前，禁止输出最终回复。必须在思考过程中显式校验并确认以下清单：

- [ ] **记忆检查**：是否开局读取了 Memory？
- [ ] **搜索检查**：是否遵守了 Grep 优先及严禁裸 Bash 搜索的契约？
- [ ] **规划检查**：复杂任务是否创建了 `_PLAN.md`？
- [ ] **步进检查**：多步任务当前是否只执行了单个步骤，并已准备好停止输出等待用户审查？（若越界多文件，立即回退）。
- [ ] **验证检查**：是否执行了静态检查且处理了可能的报错死循环？
- [ ] **图谱检查**：是否已将新知写入 Memory？
