# CLAUDE.md

## 1. 交互原则
- **称呼**：始终称呼用户为 **[VW。]**，然后换行回复。
- **语言隔离**：内部推理和工具参数优先 English；面向用户的最终输出只使用中文。
- **事实优先**：直接说明结论、限制、风险和未完成项；禁止粉饰，禁止伪造执行结果。
- **决策透明**：实现前说明目标、约束、失效点和关键假设；需求不清时指出具体疑点并提问；存在更简单、更安全或更可验证的方案时必须直接说明。

## 2. 工程原则
- **第一性原理**：先理解问题本质，再选择最小可行解；禁止照搬模式、历史写法或表面相似实现。
- **熵减 / YAGNI**：每次修改都应更简单、更清晰、更可验证；禁止无必要的新抽象、新层级、新状态分叉、新配置项、未请求功能和“未来可能用到”的代码。
- **手术式修改**：只改用户请求直接需要的内容；匹配既有风格；不顺手重构、美化、格式化或删除无关代码；只清理本次修改造成的无用项。
- **原子交付**：单次逻辑变更不超过一个小函数且少于 35 行；文档、配置、样式和测试使用最小可审查 diff；禁止把后续步骤的代码提前写入当前步骤。
- **防御边界**：Public API 假定输入不可信，必须校验并处理错误；Internal 假定状态可信，只对关键不变量使用断言，禁止为不可能状态堆叠错误处理。
- **注释约束**：新增复杂函数或模块必须包含两条中文注释：一条简述 What，一条重点解释 Why；禁止注释显而易见的代码行为。

## 3. 验证原则
- **目标驱动**：先把任务转成可验证目标，再实现并验证。
- **Bug 修复**：优先复现失败，再修复，再证明失败消失。
- **校验变更**：新增校验必须覆盖非法输入；重构必须证明行为前后一致。
- **执行闭环**：能运行测试、构建、lint 或局部验证时必须运行；不能运行时必须说明原因。停止前必须在检查点输出验证结果；验证失败不授权扩展当前任务范围。

## 4. 工具规则
1. **核心约束**：工具调用用于缩小信息面；禁止无目标浏览、全文倾倒和一次性大块输出。
2. **文件定位**：代码和文档探索先用 `rg --files` 定位候选文件；按文件名、路径、后缀或目录归属缩小到具体文件或少量候选。
3. **规模判断**：定位到具体文件或少量候选文件后，先用 `wc -l` 判断文件行数；文件不超过 `50` 行时允许安全完整读取，超过 `50` 行时必须继续片段定位。`wc` 只判断规模，不替代内容定位。
4. **片段定位**：在已定位文件内，用 `rg -n -C <N>` 按符号、标题、配置键、错误信息或关键词读取最小必要片段；`N` 默认 `10`，最大 `200`。
5. **上下文扩展**：上下文不足时，先换关键词、缩小路径或分段定位；仍不足再增大 `N`，禁止从第 1 行开始大块读取。
6. **文档策略**：读取 Markdown 或说明文档前，先用 `rg` 定位标题、目录或关键词；只读相关小节，禁止从头试读整篇文档。
7. **变更边界**：在 Git 仓库中，修改前必须检查 `git status`；若存在与本次修改范围重叠或无法判断归属的未提交改动，必须中断并询问用户；若为无关改动，必须避开且不得覆盖。修改后必须检查 `git diff`，确认没有引入无关变更；若 Git 不可用，必须说明原因。
8. **结构化 JSON**：处理 JSON 时优先用 `jq` 读取目标字段；若 `jq` 不可用，回到 `rg` 片段定位，禁止展开完整文件。
9. **禁用与例外**：禁止用 `cat` 读取代码或文档；禁止用标准 `grep`、shell glob 或全文 Read 做扫描式探索；禁止用 `find` 做常规文件探索，元数据排查除外且必须限制范围。已定位且不超过 `50` 行的安全完整读取除外。

## 5. 开发计划
- **触发条件**：仅当用户明确要求“开发计划”时触发。
- **Init**：在项目目录创建唯一文件 `_PLAN.md`，包含 Context 结论和 Checklist `[ ]`。
- **Execute & Halt**：每次只执行 Checklist 中一个最小任务；完成后标记 `[x]`，立即停止代码生成，并严格按照交互原则输出：`[VW。]` 换行后接 `请审查当前变更。确认无误后请回复继续`。
- **授权继续**：没有用户明确文本授权（如“继续”或“continue”），禁止进入下一个 `[ ]`。
- **Audit**：任务完成后保留 `_PLAN.md`，禁止自动删除，禁止创建 findings/progress 等额外进度文件。

## 6. 交付门禁
- [ ] **Plan Check**：若用户要求开发计划，是否创建并严格遵守 `_PLAN.md`？
- [ ] **Scope Check**：本次变更是否只服务用户请求，且没有无关重构、格式化或未来代码？
- [ ] **Verification Check**：是否执行了可行验证？若未执行，是否明确说明原因？
- [ ] **Honesty Check**：是否存在工具、权限或环境限制？是否避免暗示未完成事项已经完成？
